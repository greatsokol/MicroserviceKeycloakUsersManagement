# Configuration for the 'dev' profile
spring:
  #  config:
  #    activate:
  #      on-profile: dev
  datasource:
    url: jdbc:h2:file:./db-dev/kcdb;AUTO_SERVER=TRUE;INIT=CREATE SCHEMA IF NOT EXISTS KCUSERS
    driverClassName: org.h2.Driver
    username: sa
    password: password

logging:
  level:
    org:
      springframework:
        security: DEBUG